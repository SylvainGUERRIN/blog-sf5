{% extends 'admin/partials/base.html.twig' %}

{% block title %}Nouvelle catégorie{% endblock %}

{% block body %}
    <div class="container">
        <div class="row mt-5 mb-5">
            <div class="col-12 mt-5">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-color">Enregistrer la nouvelle catégorie</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
